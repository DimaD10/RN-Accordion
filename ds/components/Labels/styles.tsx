import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 8,
    width: "100%"
  },
  label: {
    paddingHorizontal: 20,
    paddingVertical: 7,
    borderRadius: 200,
    color: "#ffffff"
  }
})
